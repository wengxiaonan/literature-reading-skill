---
name: literature-reading
description: 学术文献精读技能（中文输出）。负责把 PDF / DOCX / Markdown / 纯文本文献提取为可读文本，并输出结构化精读报告（基本信息、研究问题、方法、数据、核心发现、结论、创新点、局限、可复现性），支持按章节精读、术语问答、文献对比与笔记整理。触发场景：用户给出文献文件路径并要求“读文献/精读/总结/写笔记/对比文献”，或询问某篇论文的研究方法、结论、创新点。
license: MIT
---

# 文献阅读（Literature Reading）

把一篇学术文献转化为结构化的中文精读成果。适用于单篇精读、多篇对比、术语解释、方法与数据核对、论文笔记等场景。

## 工作流程

### 0. 输入确认
- 确认用户给出的文件路径是否存在（用 `pwsh` 的 `Test-Path` 检查），并记录文件类型（.pdf / .docx / .md / .txt）。
- 询问用户本次目标（若未说明）：全篇精读、只看方法/结论、文献笔记、多篇对比、术语问答等。

### 1. 文本提取
- **PDF**：优先用 Python 3.13 + pypdf 提取全文。本机可用：
  - `py -3 -m pip install pypdf`（如已装可跳过）
  - 或 `uv run --with pypdf --no-project python ...`（uv 缓存目录若报错，先设置 `$env:UV_CACHE_DIR = "F:\deepseek harness\.uv-cache"`）
  - 提取脚本要点：按页循环 `reader.pages[i].extract_text()`，每页加 `===== PAGE N =====` 分隔符，输出 UTF-8 文本文件供 `read` 工具读取；打印总页数与总字符数。
- **DOCX**：用 python-docx 或 pandoc（若可用）提取段落与表格文本。
- **MD / txt**：直接读取。
- **扫描版 PDF（无文字层）**：提示用户，说明需 OCR（本机未配置 OCR 流程时不强行处理）。
- 提取结果写入工作区文件（如 `paper_extract.txt`），再通过 `read` 工具分块阅读（大文件按 offset/limit 分段）。

### 2. 结构化精读
按以下顺序阅读并记录：
1. **基本信息**：标题、期刊/会议、年份、卷期页码、DOI、作者与单位、通讯作者、投稿/接收时间、资助信息。
2. **摘要与关键词**：复述研究目的、方法、主要结果与结论。
3. **引言与文献综述**：研究背景、要解决的问题、现有研究的不足、本文贡献与创新点。
4. **方法与模型**：数据来源与口径、模型设定与假设、变量/参数含义与取值依据、分析步骤（如演化博弈、计量模型、仿真参数设置）。
5. **结果与分析**：主要图表含义、核心数值结果、稳健性/敏感性分析。
6. **讨论与结论**：主要结论、政策/管理启示、局限性与未来方向。
7. **可复现性**：模型公式、参数表、软件（如 Matlab 版本）、数据可得性声明。

### 3. 输出形式
根据用户目标输出：
- **精读报告**：用下面的模板，中文撰写，Markdown 格式；如有需要可保存为 `.md` 文件。
- **文献笔记**：要点式，突出可引用的结论与数据。
- **问答**：针对具体问题（方法、结论、某段话含义、参数来源）精确回答，并标注出处（章节/页码）。
- **对比分析**：多篇文献时，用表格对比（研究对象、数据、方法、结论、局限）。

### 4. 质量要求
- 引用文中数据、参数、结论时注明来源位置（如“第 X 页/第 X 节”），不凭空捏造。
- 原文为英文时，术语首次出现给出英文原文（如 同行激励 peer incentive）；公式可用文字+常见符号说明。
- 明确指出论文的局限与你的阅读疑问，方便用户后续追问。

## 精读报告模板

```markdown
# 《论文标题》精读报告

## 1. 基本信息
- 期刊/来源 / 年份 / 卷期页码 / DOI
- 作者 / 单位 / 通讯作者
- 资助与数据可得性

## 2. 研究背景与问题
## 3. 研究问题与假设
## 4. 方法与数据
## 5. 主要结果与发现
## 6. 结论与政策/管理启示
## 7. 创新点与贡献
## 8. 局限性与开放问题
## 9. 可复现性（公式/参数/软件/数据）
## 10. 阅读疑问（可追问点）
```

## 注意事项
- 大文件务必分段阅读，避免一次性读入过多内容。
- 提取失败时先诊断（缺库、路径、编码），不要反复重试同一命令；必要时换方案并说明。
- 本技能专注“阅读与分析”，如需将文献内容做成公众号文章/PPT/综述，由用户另行指定对应技能。
